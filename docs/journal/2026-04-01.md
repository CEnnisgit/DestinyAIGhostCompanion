# 2026-04-01 — Phase 3A Steps 1–6 Implemented, Spec Audit Complete, Module Restructured

## Context

Yesterday's evening session executed all 6 steps of the module reorganization. Today's morning session completed a systematic file-by-file review of all 58 changed files, organized into 6 groups by bounded context. Found and fixed several stale references that survived the initial reorg.

The afternoon session transitioned into Phase 3A preparation — context transfer to a new conversation, deep alignment on the identity model, correction of understanding gaps, and finalization of a 9-step implementation plan.

The evening session executed Steps 0–6 of the implementation plan, performed a formal spec-compliance audit, fixed identified issues, and restructured the IAM module into DDD-aligned folders.

## What Got Done

### Morning Session: Reorg Review Matrix (All 6 Groups)

Created a review matrix grouping all 58 files by bounded context (domain → db → api per group). Reviewed each group systematically:

| Group | Files | Issues Found | Action |
|-------|-------|-------------|--------|
| 1. Composition Root | 5 | `crm_repo` stale var; `tenant.rs` bundled two concerns | Renamed var; split into `clients.rs` + `saved_buildings.rs` |
| 2. Shared Kernel | 2 | None | Clean |
| 3. IAM | 2 | None | Clean (empty shells) |
| 4. Directory | 12 | 16 stale CRM refs in doc comments + `CrmState` type alias | Batch replaced all; `CrmState` → `DirectoryState` |
| 5. Operations | 20 | 2 stale doc comments (`tenant module`, `CRM/Clients` path) | Fixed both |
| 6. Workflows/LL152 | 17 | None | Clean |

**Lesson learned:** ripgrep misses non-ASCII encoded files on Windows. PowerShell `Select-String` catches what grep doesn't — use both for thorough audits.

### Afternoon Session: Phase 3A Context Transfer, Alignment & Planning

1. **Context transfer** — New conversation for Phase 3A implementation. Transferred all 20 artifacts from the previous session (`cc38ee30`). Reviewed journal entries 03/26 through 04/01, roadmap status, and the phase3_implementation_matrix.

2. **"Tenant" concept genealogy** — Traced the evolution of "tenant" through the project:
   - Phase 1 (03/18): Tenant = Company (`company_id` scopes everything)
   - Phase 1.5 (03/26): Tenant refined, `TenantAsset` retired for client-centric model
   - Phase 3 design (03/29): Person-first pivot, `company_id` no longer universal isolation key
   - Phase 3 design (03/30): Workspace abstraction replaces `company_id` (ADR-0030)
   - Module reorg (03/31): `tenant` module merged into `operations`, all "tenant" references purged

3. **Identity model understanding check** — Created formal alignment document covering User, Company, Team concepts. Quiz revealed misunderstanding of TECHNICIAN workspace interaction.

4. **ADR-0034 correction** — Updated identity model to reflect:
   - TECHNICIANs never switch workspaces — unified "My Jobs" view
   - Frontend silently sets `X-Workspace-Id` per-request based on job context
   - Only ADMINs explicitly switch workspaces for management operations
   - RLS enforces workspace isolation; application code enforces role-based visibility

5. **Implementation matrix updated** — ADR-0033 and ADR-0034 promoted to MUST-read for 3A. ModuleStructure.md added as prerequisite. Reading order expanded to 13 items. Stale spec_placement_matrix identified (artifact copy is pre-reorg; repo ModuleStructure.md is authoritative).

6. **Implementation plan finalized** — 9-step plan with 3 design decisions resolved:
   - **Q1:** `User::new()` takes `personal_workspace_id` from caller (SRP)
   - **Q2:** `companies` table has no CREATE TABLE anywhere in code — we create it fresh in IAM
   - **Q3:** Incremental wiring (each step compiles and tests independently)
   - **Session 0 added:** `company_id` → `workspace_id` rename across 10 files (~88 refs) as first step

7. **Key discovery:** No `CREATE TABLE companies` exists anywhere in the Rust codebase. The table was created manually/externally. LL152 only ALTERs it. ModuleStructure.md's "move CREATE TABLE from LL152" recommendation was based on a stale assumption.

### Evening Session: Phase 3A Steps 0–6 Implementation

Executed all domain entity steps in a single session:

| Step | Deliverable | Tests |
|------|-------------|-------|
| Session 0 | `company_id` → `workspace_id` rename (88 refs across 10 files) | ✅ 116 passing |
| Step 0 | IAM schema bootstrap (5 tables: workspaces, companies, users, company_memberships, lmp_credentials) | ✅ 116 passing |
| Step 1 | Email Value Object (6 invariants, trim+lowercase normalization, custom Serde) | +17 tests |
| Step 2 | DisplayName Value Object (2 invariants, trim+whitespace-collapse, custom Serde) | +13 tests |
| Step 3 | User Aggregate (identity-only scope, factory, update, deactivate/reactivate) | +11 tests |
| Steps 4–6 | Company Aggregate, CompanyMembership (infra-only), LmpCredential Entity | +27 tests |
| **Total** | **6 entities, 5 DB tables, 68 new tests** | **184 passing** |

### Evening Session: Spec Audit + Fixes + Restructure

1. **Formal spec-compliance audit** — Line-by-line comparison of all 6 specs against implementations. Results:
   - **1 bug found:** `companies.workspace_id` missing `NOT NULL` in DB schema
   - **5 intentional deviations** documented (factory params, infallible return types)
   - **4/6 fully compliant**, 2 compliant with documented deviations

2. **Bug fix:** Added `NOT NULL` to `companies.workspace_id` in DB schema.

3. **Spec alignment:** Changed `User::update_name()` and `User::update_email()` to return `Result<(), UserError>` per spec (future-proofing).

4. **IAM module restructured** into DDD-aligned folders:
   ```
   iam/
   ├── mod.rs
   ├── people/         (User, Email, DisplayName, CompanyMembership — inline tests)
   ├── company/        (Company aggregate — separate tests.rs)
   └── lmp_credential/ (LmpCredential entity — separate tests.rs)
   ```

## Commits Today

| Hash | Description |
|------|-------------|
| `f952373` | Fix: replace remaining CRM references in doc comments + type aliases |
| `d93f374` | Fix: update stale doc comments in operations module |
| `a22e663` | fix(iam): spec compliance — NOT NULL on companies.workspace_id, Result returns on User commands |
| `37c6031` | refactor(iam): restructure into DDD-aligned folders — people/, company/, lmp_credential/ |

## Branch

`phase3a/data-foundation` — clean working tree, all 184 domain tests passing

## Alpha Critical Path Status

```text
3A: Identity Foundation    → Steps 0–6 ✅, Step 7 🔲 ← NEXT
3B: Authentication         → Spec ✅ COMPLETE
3C.1: Authorization Core   → Research ✅, Spec 📋
```

## Implementation Plan (9 Steps)

```text
Session 0: company_id → workspace_id rename          ✅ DONE
Step 0:    Workspaces + companies tables              ✅ DONE
Step 1:    Email VO                                   ✅ DONE
Step 2:    DisplayName VO                             ✅ DONE
Step 3:    User aggregate                             ✅ DONE
Step 4:    Company aggregate                          ✅ DONE
Step 5:    CompanyMembership (infra-only)              ✅ DONE
Step 6:    LmpCredential entity                       ✅ DONE
Step 7:    DB repositories + API routes               🔲 ← NEXT
```

## Open Questions (Carry Forward)

1. **Teams workspace model** — Do teams get their own workspace (per ADR-0030) or are they a coordination layer on the admin's personal workspace (per `person_vs_company_model.md`)? Tension needs resolution before Beta.
