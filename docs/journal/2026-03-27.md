# 2026-03-27 — Phase 1.5 Complete + Phase 2 Design Decisions

## Context

Continuing from yesterday's massive session (tenant foundation, Portfolio UI, ADR-0023/0024). Phase 1.5 is ~90% done — the remaining work is the Job schema update and creation form (ADR-0023).

Phase 2 (LL152 Workflow) research is ~60% complete per the implementation matrix. Extensive design docs exist (branch topology, GPS1/GPS2 specs, findings model, workflow state machine) but 14 research questions remain open and no code exists yet.

## Carryover from 2026-03-26

| Item | Status | Priority |
|---|---|---|
| Job schema update (`building_id` nullable + `address` field) | ✅ Done (`08dd926`) | 🟠 Finish Phase 1.5 |
| Job creation form (4-field notebook form per ADR-0023) | ✅ Done (`5f143f9`) | 🟠 Finish Phase 1.5 |
| Phase 2 domain research | ✅ Started (design questions) | 🟡 Next session |
| Alpha build on web-dashboard prototype | Deferred | 🔵 After Phase 2 spec |

## What Got Done

### Morning: Phase 1.5 Completion (all exit criteria met)

1. **Add Client feature** — inline form in Portfolio (`AddClientForm.tsx`), idempotent POST, dashed toggle button (`62da407`)
2. **Derived client views** — `GET /clients/:id/summary` endpoint returning job_count, buildings[], last_job_at. Summary bar in ClientDetail panel (`62da407`)
3. **Job modal redesign** — 3-section layout (Location / Client / Job Type), bidirectional Client↔Address intelligence, clearable fields, Enter-to-submit (`2631f20`, `4516312`)
4. **New job types** — Added `Emergency` and `Repair` to `JobType` enum in Rust domain. 69/69 tests pass (`2631f20`)
5. **Phase 1.5 roadmap** — Marked complete, all 8 exit criteria checked off

### Afternoon: PDA-SDD Documentation Audit & Sync

6. **PDA-SDD compliance audit** — Studied the PDA-SDD paper rules and audited all docs in `2-during-implementation/` and `3-after-implementation/` against actual code.
7. **CLD remediation** — Populated `JOB_INTAKE/CHANGELOG.md` with 5 major log entries (all 8 PDA-SDD fields each) (`e7672b7`)
8. **Tenant module walkthrough** — Created 4 new after-impl docs in `3-after-implementation/Modules/tenant/`: README, domain, api, repository (`e7672b7`)
9. **DDD spec sync** — Updated `JobType_VO_Spec.md` (Emergency/Repair promoted) and `Client_Aggregate.md` (implementation notes) (`e7672b7`)
10. **Job engine sync** — Updated `value-objects.md` and `api.md` for address-first changes (`e7672b7`)
11. **Doc↔Impl traceability matrix** — Built comprehensive matrix mapping every DDD spec and after-impl walkthrough to actual code files. Identified 5 drift items. (`f06436f`)
12. **Drift remediation** — Fixed all 5 drift items:
    - D1: `Job_Aggregate.md` — address-first fields, version 1.1→1.2
    - D2: `job-engine/domain.md` — struct 22→24 fields
    - D3: `job-engine/tests.md` — test count 47→69, 6 new entries
    - D4: `DatabaseSchema.md` — 3 new tenant tables + jobs columns
    - D5: `API_Specs.md` — 6→13 job endpoints + 10 tenant endpoints

### Evening: Phase 2 Design Decisions & Architecture

13. **Product scope reframe** — Created `ALPHA_PERSONAS_AND_SCOPE.md` defining 3 user personas (QI, Solo Plumber, Small Team Manager). Rewrote `README.md` to reflect platform as plumbing operations tool, not LL152-only. Added supersedure notices to legacy PRD docs.

14. **Squash merge** — Compressed entire `phase-1.5/dashboard-ui` branch (200+ commits) into one clean commit on `main`. Created new `phase2/ll152-workflow` branch.

15. **All 7 design questions resolved:**
    - Q1: Branch set at intake, defaults to STANDARD_INSPECTION ✅
    - Q2: Reclassification emits BRANCH_RECLASSIFIED domain event ✅
    - Q3: Branch C artifacts — deferred (insufficient evidence) ⏸️
    - Q4: Alpha scope — Branch A only, B/C deferred-but-known ✅
    - Q5: READY_FOR_REVIEW and UNDER_REVIEW are distinct states ✅
    - Q6: Findings as child entities in `inspection_findings` table ✅
    - Q7: Photos attach at both finding-level and job-level (nullable FK) ✅

16. **3 field observation discoveries** from discussing real QI workflow:
    - **Recall transition** — QI must be able to pull back a submission (`READY_FOR_REVIEW → CAPTURING`) before LMP opens it. Current competing tool locks submissions.
    - **In-app camera capture** — Prevents cross-job photo misattribution when doing 5 inspections/day
    - **Batch review flow** — QI does multiple inspections during the day, reviews at home before submitting

17. **ADR-0025: Dual-Status Model** — Documented the parallel tracks pattern where generic `job_status` (OPEN/IN_PROGRESS/COMPLETED/CANCELED) coexists with workflow-specific `workflow_status` (DRAFT/CAPTURING/READY_FOR_REVIEW/...) via the extension table. Updated Job Aggregate §14 and backfilled ADR index.

18. **OPEN_DESIGN_QUESTIONS.md** overhauled into a resolution log with all discoveries documented.

### Commits (evening session, on `phase2/ll152-workflow`)

- `4663845` docs: resolve design Q5 — review states distinct
- `1ea4cc4` docs: resolve design Q6 — findings as child entities
- `1fb185e` docs: resolve design Q7 — photos at both levels
- `2add267` docs: overhaul OPEN_DESIGN_QUESTIONS resolution log
- `4f48622` docs: ADR-0025 dual-status model

### Commits (squashed on `main`)

- `9a26a29` squash: Phase 0 through Phase 1.5

## Phase 1.5 Final Status: ✅ COMPLETE + DOCUMENTED

(Same as morning — all 8 exit criteria met, PDA-SDD compliant)

## What's Next: Phase 2 — Schema Design

All design questions are resolved. Next steps:

1. **Schema design** — `ll152_job_details`, `inspection_findings`, `inspection_photos` tables
2. **Alpha state machine** — DRAFT → CAPTURING → READY_FOR_REVIEW + recall
3. **Rust domain types** — LL152 workflow structs, finding category enum
4. **Database migration** — SQLx migration for new tables
5. **API endpoints** — LL152 workflow-specific routes
6. **GPS1 location question** — Resolve during schema design (snapshot vs pull-at-render)
