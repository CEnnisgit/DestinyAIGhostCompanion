# 2026-04-02 — Step 7: DB Repositories + API Routes

## Context

Phase 3A Steps 0–6 are complete: all 6 IAM entities implemented, spec-audited, and restructured into DDD-aligned folders. 184 tests passing. Today's goal is Step 7 — the final step of Phase 3A — adding repository traits, SQLx implementations, and API routes.

## Plan for Today

### Step 7: DB Repositories + API Routes

**Repository traits** (pcd-domain):
- `UserRepository` — save, update, find_by_id, find_by_email, list_all
- `CompanyRepository` — save, update, find_by_id
- `LmpCredentialRepository` — save, update, find_by_id, list_by_user
- `MembershipRepository` — find_by_user, find_by_company, exists (read-only)

**SQLx implementations** (pcd-db):
- `SqlxUserRepository`, `SqlxCompanyRepository`, `SqlxLmpCredentialRepository`, `SqlxMembershipRepository`

**API routes** (pcd-api):
- `GET /api/users`, `GET /api/users/:id` — read-only per roadmap
- `GET /api/company/:id`, `PATCH /api/company/:id` — profile read + update
- CRUD for `/api/lmp-credentials` — create, list, get, update, deactivate, reactivate

**Deferred decisions:**
- Seeding → deferred to Phase 3B (users need passwords from auth)
- User creation endpoint → Phase 3D per roadmap
- LMP credential list scoping → `?user_id=` query param pre-auth, replaced by auth context in 3B

## What Got Done

*(To be filled as work progresses)*

## Commits Today

*(To be filled)*

## Branch

`phase3a/data-foundation`

## Alpha Critical Path Status

```text
3A: Identity Foundation    → Steps 0–6 ✅, Step 7 🔲 ← TODAY
3B: Authentication         → Spec ✅ COMPLETE
3C.1: Authorization Core   → Research ✅, Spec 📋
```
