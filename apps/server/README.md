# Ghost Companion — Backend Server

> **Status:** 🔲 Scaffolded (Phase 4)
> **Stack:** Rust + Axum + SQLx

The composition root. This binary wires `crates/domain`, `crates/db`, and `crates/api` together and starts the axum server.

See `docs/roadmap/PHASE_4_Infrastructure/` for the full implementation plan.
