import SwiftUI

/// Destiny-flavored palette for the Ghost Companion UI.
enum GhostTheme {
    static let accent = Color(red: 0.96, green: 0.78, blue: 0.40)   // Ghost gold
    static let guardianBubble = Color.accentColor.opacity(0.18)
    static let ghostBubble = Color(white: 0.16)
    static let background = Color(red: 0.04, green: 0.06, blue: 0.10)
}
